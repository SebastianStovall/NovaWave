{
  "name": "novawave",
  "version": "1.0.0",
  "description": "### Init and Project Structure",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "npm install --prefix backend && npm install --prefix frontend --force",
    "render-postbuild": "npm run build --prefix frontend",
    "build": "npm run db-reset --prefix backend",
    "start": "npm run start-production --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
